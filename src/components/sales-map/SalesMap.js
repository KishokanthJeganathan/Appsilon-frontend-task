import React from 'react';
import GraphTitle from '../graph-title/GraphTitle';

const SalesMap = () => {
	return (
		<section>
			{' '}
			<GraphTitle title="SALES REVENUE BY COUNTRY" />
		</section>
	);
};

export default SalesMap;
